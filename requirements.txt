# ── Audio / VAD ────────────────────────────────────────────
PyAudioWPatch==0.2.12.7
numpy>=1.24
torch>=2.0
torchaudio>=2.0

# ── STT ────────────────────────────────────────────────────
faster-whisper>=1.0          # STT local (whisper_local)
openai-whisper>=20231117     # Video transcriber (batch)
websockets>=13.0             # Deepgram streaming — additional_headers kwarg needs 13.0+

# ── LLM ────────────────────────────────────────────────────
anthropic>=0.41          # 0.40 predates the Models API (client.models.list())
openai>=1.30

# ── Video ──────────────────────────────────────────────────
yt-dlp>=2024.1.0             # descarga de video/audio

# ── BD ─────────────────────────────────────────────────────
# sqlite3 viene con Python — sin dependencia externa

# ── TUI ────────────────────────────────────────────────────
textual>=0.60.0

# ── Utilitarios ────────────────────────────────────────────
python-dotenv>=1.0

# ── RAG ────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Testing ─────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
